Class EMR.DOC.DTO.CollectTemplate Extends (%RegisteredObject, %XML.Adaptor)
{

// 医生ID：

Property UserID As %String(TRUNCATE = 1);

// 模板唯一标识

Property TemplateType As %String(TRUNCATE = 1);

Property NodeID As %String(TRUNCATE = 1);

}
