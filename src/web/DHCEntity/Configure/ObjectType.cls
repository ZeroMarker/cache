Class web.DHCEntity.Configure.ObjectType Extends (%RegisteredObject, %XML.Adaptor) [ ClassType = "", Inheritance = right, ProcedureBlock ]
{

Property CTHospital As %String(TRUNCATE = 1);

Property CTLoc As %String(TRUNCATE = 1);

Property IP As %String(TRUNCATE = 1);

Property SITE As %String(TRUNCATE = 1);

Property SSGroup As %String(TRUNCATE = 1);

Property SSUser As %String(TRUNCATE = 1);

}
