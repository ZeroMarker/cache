
数据导入Excel模板使用说明:

1. 每个模板均有一行表头，切勿修改 .

2. 每个Excel只有一个工作表.

3. 全部为文本型数据。

4. 模板中日期列请使用文本格式，且仅支持YYYY-MM-DD、YYYY/MM/DD类型

5. 模板名称对应：
	HILIST.xls:		 医保目录导入模板
	HILISTCON.xls:	 医保目录对照导入模板
	DIAG.xls:		 医保诊断导入模板
	DIAGCON.xls:	 医保诊断对照导入模板
	OPRN.xls:		 医保手术导入模板
	OPRNCON.xls:	 医保手术对照导入模板