//绩效期间判断函数judge = function(period){	//取字符串长度	var len=period.length;	if(len==6){		//取字符串前两位		var subStr3=period.substr(0,2);		if(subStr3!="20"){			return false;		}else{			//取字符串后两位			var subStr1=period.substr(5,1);			var subStr2=period.substr(4,1);			if(subStr1>2){				if(subStr2!=0){return false;}				else{return true;}			}else{				if(subStr2>1){return false;}				else{return true;}			}		}	}else{		return false;	}}