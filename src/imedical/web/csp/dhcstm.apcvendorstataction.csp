<csp:content charset="UTF-8">
<SERVER>
s Action=$Get(%request.Data("actiontype",1))
s Start=$Get(%request.Data("start",1))
s Limit=$Get(%request.Data("limit",1))
s Sort=$Get(%request.Data("sort",1))
s Dir=$Get(%request.Data("dir",1))
;

i Action = "VendorStk" d
	.S StrParam=$Get(%request.Data("Params",1))
	.w ##class(web.DHCSTM.ApcVendorStkStat).ApcVendorStk(Start,Limit,Sort,Dir,StrParam)
	.
i Action = "VendorBan" d
	.S StrParam=$Get(%request.Data("Params",1))
	.w ##class(web.DHCSTM.ApcVendorStkStat).Batch(Start,Limit,Sort,Dir,StrParam)
	.

</SERVER>
